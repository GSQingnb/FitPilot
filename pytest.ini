[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    integration: tests that require running external services (Docker, database)
    llm: tests that require a configured external LLM API
